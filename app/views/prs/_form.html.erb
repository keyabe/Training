<%= form_with(model: pr) do |form| %>
  <% if pr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pr.errors.count, "error") %> prohibited this pr from being saved:</h2>

      <ul>
        <% pr.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
